{
  "name": "backlog-ticket-bot",
  "version": "1.0.0",
  "description": "メール・Slackからの起票をAIで整理してNulab Backlogに自動登録するボット",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp -r node_modules dist/",
    "deploy": "npm run build && sam deploy",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-ses": "^3.700.0",
    "mailparser": "^3.7.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/mailparser": "^3.4.5",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
